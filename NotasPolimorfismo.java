package poo;

public class NotasPolimorfismo {

    /*
    for(int i =0; i< tE; i++){
        if(empleados[i] instanceOf Admin)
            bobthebuilder.append(empleados[i].getNombre()).append("--").append(((Admin)empleados[i]).getDepto());
            // en el primero no necesite poner (Admin) empleados[i] porque ese metodo viene heredado es general
            // en el segundo si necesite poner la operación porque ese metodo es particular

    }
    Empleado [] empleados = new Empleado[100];
    tE = 0;

    // para el alta:

    empleados[tE] = new Administrativo();


    // con try catch

    for(int i =0; i< tE; i++){
    try{
    Administrativo ad = (Administrativo) empleados[i];
    sB.append(ad.getNombre()).append(ad.getDepto());
    } catch(Exception e){

    }

     */
}
